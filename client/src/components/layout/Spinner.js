import React from "react";
import { Spinner } from "react-bootstrap";

const _Spinner = () => {
  return <Spinner animation="border" variant="dark" className="loading" />;
};

export default _Spinner;
